<!--pages/mapdetail/mapdetail.wxml-->
<map markers="{{markers}}" latitude="{{lat}}" longitude="{{lng}}"></map>
<button bindtap="gotoHere" type="primary" data-name="{{name}}" data-lat="{{lat}}" data-lng="{{lng}}">到这里去</button>